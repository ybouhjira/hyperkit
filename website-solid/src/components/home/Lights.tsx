/** macOS traffic-light dots used by every window-chrome panel on the homepage. */
export function Lights() {
  return (
    <div class="lights">
      <i />
      <i />
      <i />
    </div>
  );
}
